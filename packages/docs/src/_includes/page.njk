<!DOCTYPE html>
<html lang="en-US">
    <head>
        {% include 'meta.njk' %}

        <link rel="shortcut icon" type="image/svg" href="/assets/icon-horologist.svg"/>

        <title>Horologist | {{ title }}</title>
        <link rel="stylesheet" href="/styles/main.css"/>

        {% if variant === 'landing' %}
            <link rel="stylesheet" href="/styles/pages/index.css"/>
        {% endif %}
    </head>
    <body>
        <main>{{ content | safe }}</main>

        {% if variant === 'landing' %}
            <script src="js/index.js" type="module"></script>
        {% endif %}
    </body>
</html>