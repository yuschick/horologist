(function() {
  "use strict"
  const util = require('./util');
  const HeaderWatch = require('./site-modules/HeaderWatch');
  const DemoWatches = require('./site-modules/DemoWatches');
  const ComplicationsNav = require('./site-modules/ComplicationsNav');
  const DocsPage = require('./site-modules/DocsPage');
})();
